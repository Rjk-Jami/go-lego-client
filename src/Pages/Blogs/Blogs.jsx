import React from 'react';
import useLegoTitle from '../../hooks/useLegoTitle';

const Blogs = () => {
    useLegoTitle('Blogs')

    return (
        <div>
           <h2>blogs </h2> 
        </div>
    );
};

export default Blogs;