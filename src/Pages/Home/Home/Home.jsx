import React from 'react';
import useLegoTitle from '../../../hooks/useLegoTitle';

const Home = () => {
  useLegoTitle('Home')

    return (
        <div>
          <h2>home</h2>  
        </div>
    );
};

export default Home;